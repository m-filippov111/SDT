**Рубрика оценивания — КИМ-3.1. Архитектура приложения и организация кода**

|     |     |     |     |     |     |
| --- | --- | --- | --- | --- | --- |
| №   | Критерий | Вес | Базовый (60–79%) | Базовый + (80–89%) | Базовый ++ (90–100%) |
| 1   | Структура проекта | 25% | Базовая структура есть, но не полностью соответствует слоистой архитектуре (например, всё в одном файле или нет разделения на слои) | Структура соответствует слоистой архитектуре (routes, models, schemas разделены), пакеты организованы логично | Чёткая слоистая архитектура (api, core, schemas, services, models), правильная иерархия пакетов, есть \__init_\_.py, импорты организованы |
| 2   | Настройка виртуального окружения | 15% | Виртуальное окружение создано, но не добавлено в .gitignore или есть лишние файлы в репозитории | Виртуальное окружение создано и добавлено в .gitignore, зависимости установлены | Виртуальное окружение настроено, .gitignore корректный (venv/, pycache/, .env), есть инструкция по активации в README |
| 3   | Управление зависимостями | 15% | requirements.txt есть, но содержит не все зависимости или есть лишние пакеты | requirements.txt содержит все необходимые зависимости (fastapi, uvicorn, pytest) | requirements.txt полный, версии зафиксированы, есть разделение на основные и dev-зависимости (опционально) |
| 4   | Работающее приложение | 25% | Приложение запускается, но health-check эндпоинт не работает или возвращает некорректный ответ | FastAPI запускается без ошибок, health-check эндпоинт (/health или /) работает и возвращает 200 | Приложение запускается стабильно, health-check работает, есть автодокументация (/docs), структура запуска описана в README |
| 5   | Тесты | 10% | Тест написан, но не выполняется или падает с ошибками | Тест для health-check написан и выполняется успешно | Тест написан с использованием pytest, проверяет статус-код и содержимое ответа, есть инструкция по запуску тестов |
| 6   | Документация | 10% | Файл docs/architecture.md есть, но содержит минимальное описание или не соответствует реальной структуре | docs/architecture.md описывает архитектуру, есть схема слоёв и описание компонентов | Документация полная: описание слоёв, диаграмма (Mermaid/ASCII), объяснение потоков данных, примеры использования |

**Рубрика оценивания — КИМ-3.2. Введение в FastAPI и основы REST API**

|     |     |     |     |     |     |
| --- | --- | --- | --- | --- | --- |
| №   | Критерий | Вес | Базовый (60–79%) | Базовый + (80–89%) | Базовый ++ (90–100%) |
| 1   | Базовое приложение | 20% | Приложение запускается, но не все эндпоинты работают или есть ошибки в Swagger | Все эндпоинты работают корректно, Swagger доступен, структура проекта логична | Отличная структура (роутеры, модели, схемы разделены), автодокументация настроена, есть README |
| 2   | CRUD-эндпоинты | 30% | Реализованы 2–3 метода из 4, есть ошибки в статус-кодах или ответах | Все 4 метода (GET all, GET one, POST, DELETE) работают, возвращают корректные статус-коды | CRUD работает идеально, добавлена обработка 404, корректные статус-коды (200/201/204), есть примеры в Swagger |
| 3   | PUT и PATCH | 20% | Реализован только один метод обновления, либо оба работают одинаково | Оба метода реализованы, но есть мелкие недочёты в обработке отсутствующих полей | PUT полностью заменяет объект, PATCH обновляет частично, корректная обработка 404 и валидация |
| 4   | Фильтрация | 15% | Фильтр реализован, но работает некорректно (регистр, частичное совпадение) | Фильтр по автору работает, возвращает корректные результаты | Фильтр работает с учётом регистра/частичного совпадения, обрабатывается пустой результат, есть документация |
| 5   | Валидация | 15% | Базовая валидация через Pydantic, но не все поля проверены | Валидация типов и обязательных полей работает, возвращаются ошибки 422 | Полная валидация с ограничениями (min/max, regex), понятные сообщения об ошибках, кастомные валидаторы |

**Рубрика оценивания — КИМ-3.3. Валидация данных и продвинутая работа с FastAPI**

|     |     |     |     |     |     |
| --- | --- | --- | --- | --- | --- |
| №   | Критерий | Вес | Базовый (60–79%) | Базовый + (80–89%) | Базовый ++ (90–100%) |
| 1   | Схемы Create/Read/Update | 20% | Схемы разделены, но есть дублирование полей или отсутствуют некоторые схемы | Есть отдельные схемы Create, Read, Update, поля корректно распределены | Чёткое разделение схем, используется наследование (Base), Read не содержит чувствительных данных, есть ResponseModel |
| 2   | Пользовательские валидаторы | 25% | Валидаторы есть, но проверяют только базовые условия (например, длина) | Реализованы валидаторы для пароля (сложность) и username (формат), используются @field_validator | Валидаторы покрывают все требования: regex для username, проверка сложности пароля, понятные сообщения об ошибках, используется ValidationInfo |
| 3   | Annotated-параметры | 20% | Annotated используется, но не для всех параметров или без Query/Path | Параметры запроса и пути используют Annotated с Query/Path, есть описания | Все параметры типизированы через Annotated, есть алиасы, валидация (gt, lt, min_length), описания для Swagger |
| 4   | Вложенные схемы | 20% | Вложенные схемы созданы, но структура некорректна или не используется в эндпоинтах | Вложенные схемы работают, данные корректно сериализуются/десериализуются | Сложная вложенная структура (например, User → Address → Phone), используются model_config, валидация вложенных полей |
| 5   | Обработка ошибок | 15% | Есть базовый обработчик, но возвращает стандартный ответ FastAPI | Реализован кастомный обработчик для ValidationError или HTTPException | Кастомные обработчики для нескольких типов исключений, единый формат ошибок, логирование, документация в Swagger |

**Рубрика оценивания — КИМ-3.4. Инъекция зависимостей и чистая архитектура**

|     |     |     |     |     |     |
| --- | --- | --- | --- | --- | --- |
| №   | Критерий | Вес | Базовый (60–79%) | Базовый + (80–89%) | Базовый ++ (90–100%) |
| 1   | Структура проекта | 20% | Есть разделение на папки, но слои смешаны (например, бизнес-логика в роутерах) | Чёткое разделение на слои (api/, core/, infrastructure/), зависимости идут в правильном направлении | Идеальная слоистая архитектура, зависимости инвертированы, есть \__init_\_.py с экспортами, нет циклических импортов |
| 2   | Реализация доменного слоя | 20% | Сущности есть, но бизнес-логика частично в сервисах или роутерах | Сущности описаны корректно, бизнес-логика вынесена в доменный слой | Сущности — чистые Python-классы без зависимостей от фреймворка, бизнес-правила инкапсулированы, есть value objects |
| 3   | Сервис и репозиторий | 20% | Сервис и репозиторий есть, но работают напрямую без интерфейсов | Реализованы интерфейсы (абстрактные классы/протоколы), DI настроен | Интерфейсы через Protocol или ABC, сервисы зависят от абстракций, репозиторий изолирован, легко заменить реализацию |
| 4   | Настройка зависимостей | 15% | Depends() используется, но провайдеры не организованы | Depends() настроен корректно, есть провайдеры для сервисов и репозиториев | Провайдеры вынесены в отдельный модуль (dependencies.py), есть фабрики, настроены scope (request, singleton), есть override для тестов |
| 5   | Маршруты (эндпоинты) | 15% | Эндпоинты работают, но содержат бизнес-логику | Все эндпоинты работают, используют DI, делегируют логику сервисам | Тонкие контроллеры, только оркестрация запросов, корректные статус-коды, обработка ошибок через сервисы |
| 6   | Тестирование | 10% | Тесты есть, но без моков или падают | Написаны тесты с моками репозитория, проверяется бизнес-логика | Полное покрытие тестами с моками, используется app.dependency_overrides, тестируются сервисы и эндпоинты отдельно |

**Рубрика оценивания — КИМ-3.5. Работа с базой данных из приложения**

|     |     |     |     |     |     |
| --- | --- | --- | --- | --- | --- |
| №   | Критерий | Вес | Базовый (60–79%) | Базовый + (80–89%) | Базовый ++ (90–100%) |
| 1   | Модели и база данных | 20% | Модели созданы, но engine настроен без пула или с дефолтными параметрами | Модели корректны, engine настроен с пулом соединений (pool_size, max_overflow) | Модели с правильными связями, engine оптимизирован, есть sessionmaker, сессии управляются через DI |
| 2   | Миграции Alembic | 15% | Alembic настроен, но миграция выполнена не полностью или есть ошибки | Alembic инициализирован, миграция создана и применена, env.py настроен | Alembic полностью настроен, миграции версионируются, есть alembic upgrade head в README, .gitignore содержит \*.db |
| 3   | CRUD-эндпоинты | 20% | CRUD работает, но есть проблемы с сессиями или статус-кодами | Все CRUD-операции работают, сессии закрываются корректно | CRUD оптимизирован, используется yield для сессий, корректные статус-коды, обработка ошибок БД |
| 4   | Устранение N+1 | 20% | Эндпоинт с загрузкой связанных данных есть, но N+1 не устранён | Реализован selectinload или joinedload, проблема N+1 устранена | Используется оптимальная стратегия загрузки (selectinload для коллекций, joinedload для одиночных), есть сравнение производительности |
| 5   | Транзакции | 15% | Транзакции есть, но без обработки ошибок или отката | Реализован эндпоинт с транзакцией, есть rollback при ошибке | Транзакции с явным commit/rollback, обработка IntegrityError, атомарность гарантирована, есть логирование |
| 6   | Документация | 10% | Код частично документирован, описания эндпоинтов минимальны | Код документирован (docstrings), эндпоинты имеют summary и description | Полная документация: docstrings для моделей/сервисов, Swagger с примерами, описание связей в моделях |