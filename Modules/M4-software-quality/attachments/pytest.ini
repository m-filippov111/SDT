[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --cov=src
    --cov-report=term-missing
    --cov-report=xml

# При необходимости задайте порог покрытия раскомментировав:
# --cov-fail-under=60
